import { Configuration } from '../generated';
import fetch from 'node-fetch';
/**
 * Initialize defaults for Configuration
 *
 * @param {ConfigurationParameters} config options for Configuration.
 * @return {Configuration} Configuration object
 */
export default function apiConfiguration(config) {
    const configuration = new Configuration({
        accessToken: config === null || config === void 0 ? void 0 : config.accessToken,
        fetchApi: fetch,
        headers: Object.assign(Object.assign({}, config === null || config === void 0 ? void 0 : config.headers), { 'Authorization': `Bearer ${config === null || config === void 0 ? void 0 : config.accessToken}`, 'Passage-Version': process.env.npm_package_version || '' }),
        middleware: [],
    });
    return configuration;
}
